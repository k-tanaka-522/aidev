---
name: doc-link-check
description: 文書間リンク検査。docs/配下のMarkdownのリンク切れ・項番言及と実ファイルの不一致を検出する。Routineとして四半期ごとに定期実行する。
argument-hint: "[対象ディレクトリ or all]"
disable-model-invocation: true
---

# doc-link-check（文書間リンク検査）

> 版数: M0雛形（実装対象: docs/v2/02_実行基盤アーキテクチャ.md 5.1節・9.6節・12章。03文書8.2節への対応として新設）
> `disable-model-invocation: true` はM0限定の安全策。設計書5.1節の本来のfrontmatterは`argument-hint`のみ。
> M4でリバース生成エンジンが実装され次第、外すこと。

## 責務

文書間リンク検査（03文書8.2節が要求するリンク切れ検知、9.6節）。

## 呼び出し元・連携先

- 呼び出し元: Routines、`orchestrate`
- 連携先: `docs/00_.../00-13_課題管理表.md`

## 手順（9.6節）

1. `docs/`配下の全Markdownをgrepし、`[...](...)`形式の相対リンクを抽出する
2. リンク先パスが実在するかを確認する
3. ファイル名パターン（`{項番}-{連番}_*.md`）と本文中の項番言及（「◯◯（項番）を参照」等）を突合し、項番のみで実ファイルへのリンクが張られていない箇所を検出する

Routine（12章）として四半期ごとに定期実行し、検出結果を`00-13_課題管理表.md`へ登録する（MUST）。

<!-- M4/M5で実装: 上記1〜3の抽出・突合処理、Routine登録（cron相当） -->
