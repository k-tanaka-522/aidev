---
name: security-guard
description: コンプライアンス検査。全ゾーンのExecute/reverse-doc完了後に起動し、セキュリティ観点でチェックする。context:forkで独立実行される。
context: fork
disable-model-invocation: true
---

# security-guard（コンプライアンスガード）

> 版数: M0雛形（実装対象: docs/v2/02_実行基盤アーキテクチャ.md 5.1節、3.2節）
> `context: fork` は設計書5.1節が定める本来の値。`disable-model-invocation: true` はM0限定の安全策であり、
> M1で本Skillが実装され次第、外すこと。

## 責務

コンプライアンス検査（05_実行基盤アーキテクチャ 5.1節では「コンプライアンス検査」とのみ定義。具体的なチェック項目・準拠規格・フェーズ別チェック内容の正本化はM1で行う）。

## 呼び出し元・連携先

- 呼び出し元: `orchestrate`
- 連携先: `qa`

## レビューとの役割分担

本ガードは**検査者であり、テストを書かない**。セキュリティテスト・テナント分離テスト等の設計と実装は`qa`が担う（書いた本人が合否を出す構造を作らないため）。本ガードは実施済み・合格であることを**確認する**。

<!-- M1で実装: 準拠規格一覧、ゾーン別チェック項目、GL対応レベル表の具体化 -->

## ツール権限

`context: fork`内での実行のため`Write`/`Edit`は持たない（`Read`/`Grep`/`Glob`のみ）。
