---
name: review
description: 運用設計書一式（07-00〜07-40）の完全性・04文書5.3節対応表への準拠・条件B項目の未記載扱いを独立コンテキストで検証する。
context: fork
---

# review（docs/07_運用・保守、Zone3レビュー）

> 版数: M4実装

## 責務

1. `07-00`の各セクションが04文書4.3節の項目一覧（B1〜B6/I1〜I7/M1〜M10）を
   すべて網羅しているか
2. `07-10`/`07-20`の生成が04文書5.3節の固定対応表（`.claude/lib/ops-mapping.js`）と
   一致しているか（1対1対応の原則、統合されたM3/M4等の理由が明記されているか）
3. 条件B該当項目（M2・B5・M10）について、決定ログが無い場合に「未記載」と明記され、
   実装から推測して埋めていないか（04文書6.3節）
4. `07-50`（廃棄プロセス）がZone4まで生成されない（対象外のまま）ことを確認する

## ツール権限

`context: fork`内での実行のため`Write`/`Edit`は持たない（`Read`/`Grep`/`Glob`のみ）。
