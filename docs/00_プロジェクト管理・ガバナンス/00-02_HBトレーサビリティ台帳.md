# 00-02 HBトレーサビリティ台帳

> 列定義の正本: `docs/v2/03_成果物体系定義書.md` 3.2.1節
> 追記主体: QA（`sync-check`実行時、02文書10.1.1節・10.3節）
> 生成方式: 機構自動集計（`.claude/skills/sync-check/scripts/sync-check.js`が追記する）
> 生成ゾーン: Zone1〜（同期点A⇔B通過時に随時追記）。累積型（上限なし）。まとめ書き禁止。

`HB-ID`は複数画面をまたぐ機能／E2Eシナリオ単位のトレーサビリティ起点IDである。
同期点（ミニゲート）通過ごとに1行追記される。「逆引き列」（実装ファイル・API-ID・モジュール）は
Zone3の`traceability-reverse`が静的解析で埋める（02文書9.1.1節、Mode B入口ゲート向け）。

| HB-ID | 経路（SCR-ID/RPT-ID/BAT-ID/API-ID） | 正式要件ID | 実装ファイル（逆引き） | API-ID（逆引き） | モジュール（逆引き） | 状態 |
|---|---|---|---|---|---|---|

## 経路追加ログ

> 列定義の正本: `docs/v2/03_成果物体系定義書.md` 3.2.2節（02文書10.1.4節への対応、版1.4で新設）
> 上記本体テーブルの各行は初版登録後、書き換えない（MUST NOT）。`BAT-ID`等の後から判明した
> 経路情報はこの別テーブルへ追記専用で記録する。本体行との統合はZone3の
> `traceability-reverse`がRTM生成時に一括して行う（Zone1〜2時点では統合しない）。
> 追記主体: 機構（`sync-check`。バッチ版`--kind=batch`等）。

| HB-ID | additional_id | added_at | added_by |
|---|---|---|---|
