---
name: review
description: アプリケーション設計書一式（03-01〜03-10）の完全性・決定ログとの整合・CRUD図/静的解析結果の妥当性を独立コンテキストで検証する。
context: fork
---

# review（docs/03_アプリケーション設計、Zone3レビュー）

> 版数: M4実装（実装対象: docs/v2/02_実行基盤アーキテクチャ.md 11章）

## 責務

`reverse-doc`が生成したアプリケーション設計書一式について、`context: fork`の
独立コンテキストで以下を検証する。

1. 条件付き必須の判定（03-04/03-05/03-08/03-09）が実物の有無と一致しているか
   （`00-01`カタログの「対象外」記録が妥当か）
2. `03-10_CRUD図.md`の「残課題」に記載されたunresolved項目が、実際に静的解析の
   アダプタ範囲外（コールグラフを跨ぐ等）であるかを目視確認する
3. `03-09_バッチ設計.md`のdrawio別紙が生成されているか、生成できない場合でも
   Mermaidスケルトンが本文に残っているか（フォールバック確保、03文書8.5.2節）
4. 「2.5 決定ログからの引用」節が実在する決定ログを参照しているか

## ツール権限

`context: fork`内での実行のため`Write`/`Edit`は持たない（`Read`/`Grep`/`Glob`のみ）。

## 動作確認（M4）

PMへの最終報告を参照。
