---
name: review
description: インフラ設計書一式（04-01〜04-11）の完全性・決定ログとの整合・drawio別紙生成状況を独立コンテキストで検証する。
context: fork
---

# review（docs/04_インフラ設計、Zone3レビュー）

> 版数: M4実装（実装対象: docs/v2/02_実行基盤アーキテクチャ.md 11章）

## 責務

1. `04-01`・`04-02`のdrawio別紙が実在するか、無い場合でも本文のMermaidスケルトンが
   フォールバックとして残っているか（03文書8.5.2節）
2. IaCファイルの暫定分類（`reverse-doc`のSKILL.md参照）が実態と一致しているか
   （誤分類があれば`00-13_課題管理表.md`へ登録する）
3. `04-04`（可用性）・`04-06`（バックアップ）・`04-07`（DR）の条件付き必須判定が
   `00-01`カタログの「対象外」記録と整合しているか
4. 「2.5 決定ログからの引用」節の内容が決定ログの実在エントリと一致するか

## ツール権限

`context: fork`内での実行のため`Write`/`Edit`は持たない（`Read`/`Grep`/`Glob`のみ）。

## 動作確認（M4）

PMへの最終報告を参照。
