# aiDev プロジェクト引継ぎファイル

## プロジェクト概要

aiDevは、対話型AIを活用した「AI開発支援窓口（プリセールスエージェント）」を構築するプロジェクトです。AWS Bedrockを活用し、ユーザーとの対話を通じて開発の相談やAWS環境の設計・構築・改善の支援を行います。

## 現在の進捗状況

### 完了している項目

- プロジェクト基本構成の整備（ドキュメント、ソースコード、デプロイスクリプト）
- フロントエンドのデプロイ手順の整備
- フロントエンド機能拡張の設計（AWS固有機能表示コンポーネント）
- CI/CD環境セットアップのためのスクリプトの準備
- プロジェクト初期化用ディレクトリ構造の整理（`src/infra-init`の作成）
- AIアウトプット管理機能の基本設計（システム概要に追記）
- ドキュメントレビューの実施と課題点の特定
- AWS固有機能表示コンポーネントの実装
  - AiMessageParser: AIレスポンスの特殊フォーマット解析
  - AWSPricingDisplay: AWS料金計算結果の表形式表示
  - AWSArchitectureDisplay: インフラ構成図の視覚的表示
- 設計ドキュメントの整合性確保
  - フェーズ定義の整合性確保（各ドキュメント間の表現統一）
  - 自己進化型ナレッジベースの具体的実装設計の追加
  - S3ディレクトリ構造の詳細設計（特に企業固有知識の高解像度対応）
  - Draw.io互換形式変換の技術詳細設計の追加
  - セキュリティとマルチテナント性の両立に関する詳細設計の追加
- CI/CDデプロイ手順書の作成
  - CodeCommitリポジトリへのプッシュに基づく自動デプロイフロー
  - 初期設定手順とトラブルシューティングガイド
- ナレッジベース設計のMCPハイブリッドアクセス対応への修正
  - MCPとS3のハイブリッドアーキテクチャの詳細定義
  - AWS関連知識をS3からMCPに移行する設計の更新
  - ナレッジ抽出時のMCP参照フローの追加
  - ハイブリッドナレッジ検索フローの実装詳細
  - フェーズ別実装計画の修正
- CI/CD環境のセットアップ
  - 基本CI/CDリソーススタック（`aidev-cicd-resources`）の構築
  - CodeBuildスタック（`aidev-cicd-resources-codebuild`）の構築
  - CodePipelineスタック（`aidev-cicd-resources-codepipeline`）の構築
  - GitHub連携の設定と最小限のプロジェクト構造の実装
- 設計ドキュメントのファイル構造整理
  - 基本設計と詳細設計の番号体系の統一
  - 関連するドキュメント間の番号による関連付け
  - 詳細設計ファイルの階層構造をフラット化
  - プレフィックス番号方式による整理（例：02-01_プリセールスエージェント.md）

### 進行中の項目

- エージェント選択UI機能の拡張
- CI/CD環境のデプロイテスト実施

## 次のステップ

以下の作業が優先されるべきです：

1. **CI/CD環境のセットアップ (完了)**
   - [x] プロジェクト初期化用のディレクトリ構造の整理（`src/infra-init`の作成）
   - [x] `src/infra-init/deploy-cicd.sh`スクリプトにCloudFront無効化ステップを追加
   - [x] CI/CDデプロイ手順ドキュメントを更新
   - [x] 実際のCI/CD環境のセットアップと動作確認
     - [x] 基本CI/CDリソーススタック（`aidev-cicd-resources`）の作成
     - [x] 関連リソースの作成（CloudFrontInvalidationLambda等）
     - [x] CodeBuildスタックの作成（`aidev-cicd-resources-codebuild`）
     - [x] CodePipelineスタックの作成（`aidev-cicd-resources-codepipeline`）
     - [ ] デプロイテストの実施

2. **自動化および手動実行の管理設計**
   - [x] エージェントの自動実行vs手動実行の境界定義
   - [x] ユーザー権限レベルに応じた承認フロー設計
   - [x] 実行前確認と結果報告の標準化
   - [x] ユーザー成長・学習度合いに応じた柔軟な権限設計
   
3. **エージェント選択UI機能の拡張**
   - [ ] より直感的なエージェント選択インターフェースの実装
   - [ ] エージェント状態の可視化機能の追加

4. **MCP API連携の詳細設計**
   - [ ] 認証・認可フローの詳細仕様
   - [ ] クエリ構築パターンの定義
   - [ ] エラーハンドリング戦略の策定
   - [ ] キャッシュ最適化設計の実装

5. **ドキュメント構造の一括移行**
   - [ ] 新ドキュメント生成ルール（`/docs/90_リファレンス/ドキュメント生成ルール.md`）に基づく移行
   - [ ] ディレクトリ構造の10単位から1単位への変更（10_要件→02_要件定義など）
   - [ ] 要件ID（FR-XXX/NFR-XXX）の導入と既存ドキュメントへの割り当て
   - [ ] トレーサビリティマトリクスの作成
   - [ ] 移行スクリプトの作成および実行
   - [ ] 参照リンクの更新

## 重要なファイルと場所

- **プロジェクト概要**: `/mnt/c/dev2/aiDev/README.md`
- **AIアウトプット管理機能設計**: `/mnt/c/dev2/aiDev/docs/20_設計/21_基本設計/01_システム概要.md`
- **環境構築自動化と実行管理設計**: `/mnt/c/dev2/aiDev/docs/20_設計/22_詳細設計/06-04_環境構築自動化.md`
- **フロントエンド拡張設計**: `/mnt/c/dev2/aiDev/docs/30_開発/02_フロントエンド機能拡張メモ.md`
- **フロントエンドデプロイ手順**: `/mnt/c/dev2/aiDev/docs/50_運用/01_フロントエンドデプロイ手順.md`
- **CI/CDデプロイ手順**: `/mnt/c/dev2/aiDev/docs/50_運用/02_CI_CDデプロイ手順.md`
- **CI/CD環境構築スクリプト（新）**: `/mnt/c/dev2/aiDev/src/infra-init/deploy-cicd.sh`
- **CI/CD環境構築スクリプト（旧・非推奨）**: `/mnt/c/dev2/aiDev/deploy-scripts/deploy-cicd.sh`
- **ナレッジベース詳細設計**: `/mnt/c/dev2/aiDev/docs/20_設計/22_詳細設計/02-06_ナレッジベース.md`
- **MCPハイブリッドアクセス設計**: `/mnt/c/dev2/aiDev/docs/20_設計/22_詳細設計/02-07_MCPハイブリッドアクセス.md`
- **Draw.io互換形式変換設計**: `/mnt/c/dev2/aiDev/docs/20_設計/22_詳細設計/03-08_DrawIO互換形式変換.md`
- **セキュリティとマルチテナント**: `/mnt/c/dev2/aiDev/docs/20_設計/22_詳細設計/06-06_セキュリティとマルチテナント.md`
- **ドキュメント整理スクリプト**: `/mnt/c/dev2/aiDev/docs/20_設計/22_詳細設計/rename.sh`
- **ドキュメント生成ルール**: `/mnt/c/dev2/aiDev/docs/90_リファレンス/ドキュメント生成ルール.md`

優先タスク:
- CI/CD環境のデプロイテストの実施
- MCP API連携の詳細設計
- エージェント選択UI機能の拡張

### 2025-04-20 (以前)
- CI/CD環境のセットアップ状況
  - 基本CI/CDリソーススタック（`aidev-cicd-resources`）は正常に作成完了
  - 関連リソースの作成も進行中（CloudFrontInvalidationLambda等）
  - CodePipelineServiceRoleの作成も進行中（CREATE_IN_PROGRESS）
  - 必要な対応：
    1. AWS CLIを使用してスタックとリソースの状態を確認する
       ```bash
       aws cloudformation describe-stacks --stack-name aidev-cicd-resources --region ap-northeast-1
       aws cloudformation list-stacks --stack-status-filter CREATE_COMPLETE CREATE_IN_PROGRESS --region ap-northeast-1
       ```
    2. GitHub接続の状態を確認し、必要に応じて手動承認を実施
       ```bash
       aws codestar-connections get-connection --connection-arn arn:aws:codestar-connections:ap-northeast-1:897167645238:connection/81b84f19-9b3e-4ee4-bd35-57418d27ce41 --region ap-northeast-1
       ```
    3. CodeBuildとCodePipelineスタックが正常に作成されたかを確認
  - 次のステップ：リソース作成完了後、実際のGitHubリポジトリへのプッシュでパイプラインが動作するかテストを実施

### 2025-04-20 (現在)
- CI/CD環境のセットアップが完了
  - 基本CI/CDリソーススタック（`aidev-cicd-resources`）が正常に作成完了
  - CodeBuildスタック（`aidev-cicd-resources-codebuild`）が正常に作成完了
  - CodePipelineスタック（`aidev-cicd-resources-codepipeline`）が正常に作成完了
  - GitHub接続が「AVAILABLE」状態に設定済み
  - 最小限のbuildspecファイルとプロジェクト構造を構築:
    - frontend/buildspec.yml
    - backend/buildspec.yml（新規作成）
    - infra/buildspec.yml
    - backend/lambda/index.js（サンプル作成）
    - backend/lambda/package.json（サンプル作成）
  - CloudFrontディストリビューションIDはダミー値（`DUMMY_ID`）で設定済み
  - 次のステップ：
    1. 実際のGitHubリポジトリへのプッシュでパイプラインが動作するかテストを実施
    2. 実際のCloudFrontディストリビューションIDを使用して無効化機能をテスト
    3. より完全なバックエンドとフロントエンドの実装を追加

- **改善提案**:
  - デプロイスクリプト（deploy-cicd.sh）にjqコマンドの存在チェックを追加
  - 以下のようなコードを追加することでjqが存在しない場合にエラーメッセージを表示して停止
    ```bash
    # jqコマンドの存在確認
    if ! command -v jq &> /dev/null; then
        echo "エラー: jqコマンドがインストールされていません。"
        echo "実行前にインストールしてください: sudo apt-get update && sudo apt-get install -y jq"
        exit 1
    fi
    ```
  - 複雑なコマンド構文を単純化し、より読みやすく安定したスクリプトにする
  - このチェックをスクリプトの初期段階（変数定義の後、本処理の前）に追加することで、実行途中での失敗を防止

### 2025-04-28
- CloudFront無効化機能のCI/CDパイプラインへの追加
  - CI/CD環境構築用CloudFormationテンプレートを修正
  - CloudFrontキャッシュ無効化用Lambda関数とIAMロールを追加
  - CodePipeline実行時のキャッシュ無効化ステップを追加
  - `deploy-cicd.sh`スクリプトに`-c`オプションを追加してCloudFrontディストリビューションIDを指定可能に
  - CI/CDデプロイ手順書にCloudFront無効化機能に関する説明を追加
  - 引継ぎファイルを更新し、CI/CD環境へのCloudFront無効化ステップ追加作業の完了を記録

### 2025-05-20
- 自動化および手動実行の管理設計を完了
  - 環境構築自動化と実行管理設計ドキュメントを作成 (`/docs/20_設計/22_詳細設計/06_機能設計/03_環境構築自動化.md`)
  - エージェントの自動実行と手動実行の境界を明確に定義
  - ユーザー権限レベルに応じた承認フローの設計（単一承認/多段階承認/条件付き自動承認）
  - 実行前確認と結果報告の標準化プロセスを定義
  - ユーザー成長・学習度合いに応じた動的権限調整メカニズムの設計
  - フェーズ別の実装計画策定（基盤構築・拡張機能・最適化）
  - 広範なセキュリティ対策と監査メカニズムの設計
  
  **実装したCloudFront無効化の主要コンポーネント**:
  1. **CloudFrontInvalidationLambdaRole**: Lambda関数がCloudFrontキャッシュを無効化するための権限を持つIAMロール
  2. **CloudFrontInvalidationLambda**: CloudFrontキャッシュ無効化リクエストを作成するNodeJS Lambda関数
  3. **InvalidateCloudFrontCache**: CodePipelineのデプロイステージに追加された無効化ステップ
  4. **CloudFrontDistributionId**: パイプラインへのパラメータとして追加されたディストリビューションID

  **機能上の改善点**:
  - フロントエンドの変更がユーザーに即時反映されるようになった
  - キャッシュによる古いコンテンツ表示の問題を解消
  - パイプライン実行中に自動的にキャッシュが無効化されるため、手動操作が不要に

- 次のステップとしての優先度:
  1. 実際のCI/CD環境のセットアップと動作確認（デプロイテスト）
  2. デプロイ/ロールバック戦略のさらなる詳細化（運用面の最優先課題）
  3. 環境変数管理メカニズムの実装（拡張性の最優先課題）

### 2025-04-27
- GitHub連携CI/CD環境の詳細レビューを実施
  - コードレビューとドキュメントレビューを実施し、改善ポイントを特定
  - 日本語環境でのAWS管理コンソール操作手順を詳細化（「CodePipeline」>「設定」>「接続」タブ）
  - デプロイフローの改善点をまとめ、修正計画を策定
  
  **CI/CD環境の修正計画**:
  1. **deploy-cicd.sh スクリプト改善**:
     - ログディレクトリパスを相対パスに変更し、環境依存を減らす
     - S3バケット作成のエラーハンドリングを強化
     - 変数名を簡潔にしてコード可読性を向上
     - コメントを追加し、各セクションの役割を明確化
  
  2. **CloudFormationテンプレート改善**:
     - IAM権限を最小権限の原則に基づいて最適化（特にログ関連権限の範囲を限定）
     - S3バケット命名ルールの検討（クロスアカウント対応）
     - CloudFrontキャッシュ無効化ステップの追加
     - 環境ごとの条件分岐とマニュアル承認ステップの追加（本番環境用）
  
  3. **ビルド環境の調整**:
     - フロントエンドビルド用のリソースサイズを調整（メモリ使用量に応じて）
     - 環境変数の動的な設定メカニズムの追加（Systems Manager Parameter Storeの活用）
     - ビルド失敗時の通知メカニズムの実装
  
  4. **デプロイ戦略の強化**:
     - ロールバック戦略の明確化と自動化
     - CloudFrontディストリビューションの無効化を含むフロントエンドデプロイ手順の追加
     - バックエンドデプロイ（Lambda/SAM）の詳細手順の追加
  
  5. **ドキュメント強化**:
     - CI/CD環境のアーキテクチャ図追加
     - 初回デプロイ成功時のチェックリスト作成
     - 各ステージ（ソース、ビルド、デプロイ）での詳細な処理フロー解説
     - マルチ環境デプロイのベストプラクティス追加

- 次のステップとしての優先度:
  1. CloudFormationテンプレートへのCloudFront無効化ステップの追加（機能面の最優先課題）
  2. デプロイ/ロールバック戦略のドキュメント化（運用面の最優先課題）
  3. 環境変数管理メカニズムの実装（拡張性の最優先課題）

### 2025-04-26
- GitHub連携のCI/CD環境セットアップ手順を整備
  - 引継ぎファイルを更新し、GitHub連携用のセットアップ手順を追加
  - CI/CDデプロイ手順書を大幅に更新し、GitHub連携に対応
  - CodeCommit関連の手順を削除し、GitHub連携の手順に置き換え
  - スクリプト実行時の具体的なパラメータ例を追加し、より実践的な手順に改善
  - GitHub接続の手動承認手順の詳細説明を追加
  - トラブルシューティングセクションを充実させ、GitHub連携特有の問題解決方法を追加
  - プロジェクト構造と必要なbuildspec.ymlファイルの配置に関する説明を更新
  - 運用上の考慮点として環境変数やシークレット管理、ブランチ保護ルールなどに関する推奨事項を追加

### 2025-04-25
- GitHubを使用したCI/CD環境用にCloudFormationテンプレートを修正
  - CodeCommitの代わりにGitHubをソースとして使用するように変更
  - AWS CodeStarConnectionsを使用してGitHubとの連携を実現
  - CI/CD環境構築スクリプト (`deploy-cicd.sh`) をGitHubパラメータに対応するよう修正
  - GitHub接続用のオプション (`-o`, `-g`, `-b`) を追加
  - レポジトリ名の指定方法を変更し、GitHub所有者とリポジトリ名を個別に指定する形式に変更
  - CodeBuildプロジェクトのソースタイプをCodePipelineに変更
  - CodePipelineソースアクションをCodeStarSourceConnectionに変更
  - スクリプトの冪等性を考慮し、複数回実行しても安全に動作することを確認


### 2025-04-23
- プロジェクト初期化用のディレクトリ構造を整理
  - `src/infra-init`ディレクトリを新設し、CI/CD環境構築用のスクリプトを移動
  - プロジェクト初期化と継続的デプロイの責務を明確に分離
  - README.mdを更新し、新しいディレクトリ構造を反映
  - CI/CDデプロイ手順ドキュメントの参照先を更新
- 引継ぎファイルを更新
  - プロジェクト初期化用ディレクトリ構造の整理を完了タスクとして追加
  - 次のステップとして`src/infra-init/deploy-cicd.sh`スクリプトを使用したCI/CD環境のセットアップを設定
  - 重要なファイルの場所情報を更新

### 2025-04-22
- ナレッジベース設計のMCPハイブリッドアクセス対応への修正を完了
  - MCPとS3のハイブリッドアーキテクチャを詳細に定義
  - AWS関連知識をS3からMCPに移行する設計に更新
  - ナレッジ抽出時のMCP参照フローを追加
  - ハイブリッドナレッジ検索フローの詳細設計を実装
  - MCPキャッシュ更新フローの処理を定義
  - フェーズ別実装計画を修正し、MCP対応内容を反映
  - まとめと次のステップセクションを更新し、MCP API連携の詳細仕様に関する次のステップを明確化
- MCPハイブリッドアクセス設計に関する専用ドキュメントを作成
  - MCPの概要とAWS Bedrockとの統合方法
  - aiDevプロジェクトにおけるMCP活用のメリットと詳細設計
  - MCPクエリパターンとキャッシュ戦略の定義
  - エラーハンドリング戦略の策定
  - フェーズ別実装計画の詳細化
- 機能設計ドキュメントの整理方針を検討
  - サブ項番を使用したファイル命名規則（例：02-1_ナレッジベース.md、02-2_MCPハイブリッドアクセス.md）
  - 関連する機能をグループ化しつつフラットな構造を維持する方式を採用
- 引継ぎファイルを更新
  - ナレッジベース設計の修正タスクを完了として更新
  - 次のステップとして、CI/CD環境のセットアップとMCP API連携の詳細設計を優先タスクとして設定

### 2025-04-21
- CI/CDデプロイ手順書を作成
  - CodeCommitリポジトリへのコード管理手順
  - AWSリソース構築から初期設定、コードプッシュまでの一連のフロー説明
  - トラブルシューティングガイドライン
- ナレッジベース設計のMCPハイブリッドアクセス対応への修正計画を策定
  - AWS関連知識はS3ではなくMCPから取得する方針
  - 企業固有知識と生成コンテンツのみをS3に保存
  - 次のステップとしてMCPとのハイブリッドアクセス設計の実装に注力する必要あり
- 引継ぎファイルを更新し、優先タスクを再整理
  - ナレッジベース設計の修正を最優先タスクとして追加
  - CI/CD環境のセットアップと実際のデプロイテストも優先度が高い

### 2025-04-20
- 設計ドキュメントの整合性確保に関する作業を完了
  - システム概要.mdのフェーズ定義を更新し、プラン.mdと統一（3フェーズ体制）
  - README.mdにも同じフェーズ定義を追加して一貫性を確保
  - 自己進化型ナレッジベースの詳細設計を作成（S3ディレクトリ構造の詳細含む）
  - Draw.io互換形式変換の技術詳細設計を作成
  - セキュリティとマルチテナント性の両立に関する詳細設計を追加
- 追加したドキュメント：
  - 02_ナレッジベース.md
  - 06_DrawIO互換形式変換.md
  - 07_セキュリティとマルチテナント.md
- 次のステップとして、CI/CD環境のセットアップとエージェント選択UI機能の拡張に注力すべき
- **重要な修正点**：ナレッジベース設計における AWS 関連知識の扱いについて修正が必要
  - 現在の設計では AWS 関連知識を S3 に直接格納する形になっているが、実際の方針では MCP（Model Customization Provider）を使用する
  - AWS サービス情報、ベストプラクティス、アーキテクチャパターンなどは MCP から取得するように修正する必要あり
  - S3 ナレッジベースは主に企業固有知識や生成コンテンツの保存に使用する
  - ナレッジベース詳細設計を修正し、MCP とのハイブリッドアクセス設計に変更する必要あり

### 2025-04-19 (夕方)
- 設計ドキュメントの整合性確保に関する現状分析を実施
  - フェーズ定義の不一致：プランでは4フェーズ、システム概要では3フェーズの記述があり統一が必要
  - ナレッジベース設計：既存のファイルはあるが空の状態、具体的な実装設計が必要
  - S3ディレクトリ構造：企業固有知識の高解像度対応を含む詳細設計が未作成
  - Draw.io互換形式変換：AIが生成した構成情報を変換するアルゴリズムの詳細設計が必要
  - セキュリティとマルチテナント：顧客データ分離の具体的アーキテクチャ設計が未定義
- 設計ドキュメント整合性確保のアプローチ案を策定
  1. フェーズ定義の整合性確保を最優先（他の設計の前提となるため）
  2. S3ディレクトリ構造の詳細設計を次に実施
  3. 自己進化型ナレッジベースの具体的実装設計
  4. セキュリティとマルチテナント性の両立に関する詳細設計
  5. Draw.io互換形式変換の技術詳細設計

### 2025-04-19 (続き)
- AWS固有機能表示コンポーネントを実装
  - AiMessageParser: AIレスポンス内の特殊フォーマットを検出して適切なコンポーネントで表示
  - AWSPricingDisplay: AWS料金計算結果を表形式で表示するコンポーネント
  - AWSArchitectureDisplay: インフラ構成図を視覚的に表示するコンポーネント（初期実装）
- ChatMessage.jsコンポーネントを更新し、AiMessageParserを統合
- 特殊フォーマット検出の正規表現処理を実装
  - ```aws-pricing```と```aws-architecture```形式のJSONデータ解析

### 2025-04-19 (午前)
- ドキュメントレビューを実施し、以下の課題を特定
  - ナレッジベースの自動更新に関する設計詳細の不足
  - S3ディレクトリ構造の明確な定義の必要性
  - フェーズ定義の不一致
  - ナレッジベース関連記述の分散と整合性の問題
  - Draw.io互換形式変換の具体的実装方法の不足
  - マルチテナント環境での顧客データ分離とセキュリティ設計の強化必要性
- プランに「自己進化型ナレッジベース」を差別化戦略として追記
  - 対話から顧客情報・要件を自動抽出・構造化
  - 人手によるメンテナンス不要の自動更新システム
  - 顧客との対話が進むほど精度が向上する学習サイクル
  - 企業固有コンテキストの自動構築と継続的拡充
- ナレッジ強化サイクルの記述を強化
  - S3ナレッジベースの自律的整理・最適化
  - 企業特性に応じた情報分類と優先度付け
- AIアウトプット管理機能の基本設計をシステム概要ドキュメントに追記
  - Draw.io互換形式での構成図出力
  - S3へのコンテンツ保存（ナレッジベースとの統合）
  - 将来的なSaaS連携（Notion、GitHub等）の計画

### 2025-04-18
- 引継ぎファイルを作成
- プロジェクト現状の整理と次のステップの特定

### 2025-04-20 (現在)
- 設計ドキュメントのファイル構造整理を実施
  - 基本設計と詳細設計の間の関連性を明確にするためのファイル命名規則を設定
  - 詳細設計の深い階層構造をフラット化し、プレフィックス番号方式で整理
  - プレフィックス番号は基本設計の番号に対応（例：02-01は基本設計02に対応する詳細設計）
  - バックアップと移行のためのスクリプトを作成し、変更を実施
  - 合計28の詳細設計ファイルを新しい命名規則で再編成
  - 引継ぎファイルの重要なファイル場所を更新
  - ドキュメント間のナビゲーションと関連性がより直感的に理解できるように改善

- 次のステップとしての優先度:
  1. CI/CD環境のデプロイテストの実施
  2. MCP API連携の詳細設計
  3. エージェント選択UI機能の拡張

### 2025-04-19
- CI/CD環境のセットアップに関する問題の解決
  - スクリプト内の問題を修正
    - jqコマンドの存在チェックを追加
    - 複雑な入れ子構文を単純な条件分岐に変更
    ```bash
    # 修正前の複雑な入れ子構文
    aws cloudformation $([[ $(aws cloudformation describe-stacks...)...
    
    # 修正後の単純な条件分岐
    if aws cloudformation describe-stacks --stack-name $STACK_NAME --region $REGION 2>/dev/null; then
      ACTION="update-stack"
    else
      ACTION="create-stack"
    fi
    ```
  - CodeBuildプロジェクト定義の問題を特定
    - `Artifacts` セクションの `Type` が "S3" になっていたが、`Source` セクションの `Type` が "CODEPIPELINE" になっていた
    - CodePipelineと連携する場合、両方を "CODEPIPELINE" に設定する必要がある
    - テンプレートを修正し、S3バケットのパラメータ指定部分を削除
  - CodeBuildスタックのデプロイに失敗
    - `ROLLBACK_COMPLETE` 状態になっているスタックを削除
    - テンプレートの検証は成功するが、スタック作成が失敗
  - 必要な対応：
    1. Windows TerminalまたはWSLから修正したスクリプトを実行する
       ```bash
       cd /mnt/c/dev2/aiDev
       ./src/infra-init/deploy-cicd.sh -e dev -r ap-northeast-1 -o k-tanaka-522 -g aidev -b main
       ```
    2. GitHub接続の承認状態を確認（現在は「利用可能」になっている）
    3. CodeBuildとCodePipelineスタックの正常な作成を確認
    4. 実際のデプロイテストを実施
  
  - 改善したコンポーネント：
    1. デプロイスクリプト（`deploy-cicd.sh`）
       - jqコマンド存在チェックの追加
       - スクリプト構文の単純化と堅牢性強化
    2. CodeBuildプロジェクト定義（`codebuild-projects.yml`）
       - Artifacts Typeの修正（S3→CODEPIPELINE）
       - CodePipelineインテグレーションの正しい設定

## 次回の開発課題

### 1. CI/CD自動化プロセスの整備
- 実行済みCLIコマンドの標準化および`infra-init`への統合
- 冪等性を担保した実行安全なスクリプト設計
- プログラムによる自動実行時の明示的承認フローの実装
- CloudFormationスタック管理のベストプラクティス文書化

### 2. ドキュメント強化
- 実行コマンドのリファレンスガイド作成
- CI/CD自動化フローの全体アーキテクチャ図
- 各ステップの実行前チェックリスト
- 環境別設定パラメータの一覧と管理指針

### 3. Bedrockエージェント開発ライフサイクル設計
- 開発〜リリースまでの標準プロセス定義
  - 要件収集（会話ログ分析）→ バックログ作成 → スプリント計画 → 実装 → テスト → デプロイ
- ターゲットユーザー層に応じた対話設計
  - エンドユーザー向け: 業務課題起点の抽象度高い対話
  - SIer向け: 技術実装詳細まで踏み込んだ対話
- ユーザープロファイルの動的管理
  - ITスキルレベルの自動判定（初級・中級・上級）
  - 業種・部門に応じた説明・例示の最適化
  - 対話履歴に基づくユーザー成長の追跡と説明レベルの段階的調整
- リリース判断基準と品質指標の策定
  - 機能完成度、テストカバレッジ、パフォーマンス指標等

### 4. 開発プロセス自動化
- プロンプト開発・評価フレームワークの構築
- ユーザー会話のテスト自動化（会話シナリオテスト）
- コードとプロンプトの連携したバージョン管理方式
- CI/CDパイプラインとの統合による継続的デプロイ
- 承認フローの実装
  - タスクレベルでの明示的承認機能
  - ユーザー権限とスキルレベルに応じた承認プロセスの最適化
  - 実行結果のわかりやすい可視化と説明
